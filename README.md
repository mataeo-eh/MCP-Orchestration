To install the required dependencies for this project open your terminal and copy/paste the following line
pip install -r requirements.txt

This is a long list of dependencies, and not all may be absolutely required for the project, but downloading them all ensures the needed dependencies will be installed.